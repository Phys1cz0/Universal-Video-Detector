# Universal Video Detector 開発ルール

基準: 現行最新版を唯一のベースとして継続更新する。既存ルール1〜110は本作業でユーザーが正式に提示した内容を整理したもの。111以降は既存の正式な追加事項を継承する。

## 1〜110 基本・実装・検証ルール

1. 現在の最新版を唯一のベースとして継続更新する。
2. 過去版へ戻して実装しない。新規プロジェクトを作成しない。
3. 小さな修正でも必ずバージョンを上げる。
4. バージョニングは数字のみで行う。
5. `0.3.53`のような数字によるバージョン番号を使用し、fixed、final、betaなどを付加しない。
6. Adapterを削除しない。
7. 既存の正常な動画検出・自動読み込みを壊さない。
8. 動画をタイトルだけで同一判定しない。
9. URL、動画識別情報、その他の複数要素を使って重複を検証し、既存リストへ累積する。
10. 以降はユーザーが更新した場合だけ再取得する。
11. 検出中は検出中であることを正しく表示する。
12. 検出完了前に完了状態へ変更しない。
13. UIには数字だけのバージョン番号を表示する。
14. 設定は保存し、次回起動時も保持する。
15. 保存先はWindowsのフォルダー選択から取得する。
16. 選択した保存先パスを編集可能なテキストボックスへ確実に表示する。
17. 保存先設定を勝手に削除・初期化しない。
18. CoAppとDownloader Workerを分離する。
19. Native Messagingの仕様に沿って動作させる。
20. HLS/DASHも動画として扱う。
21. ffmpegの探索は、設定されたパス、CoApp周辺、Windows PATHなど、決められた探索経路を正しく処理する。
22. ダウンロードタブへ勝手に遷移させない。
23. ダウンロード済み・失敗・待機中を正しく区別する。
24. 失敗したものをダウンロード済みにしない。
25. 成功済み動画を再ダウンロードするときだけ警告する。
26. サイトごとに保存フォルダーを作る。
27. サイト内の保存フォルダー名は、サイト名＋タイトル方式または連番方式を設定で選択できるようにする。
28. 同一サイト内の異なる動画が同一フォルダーへ混ざらないようにする。
29. CoAppの再インストールを前提とした不安定な設計にしない。
30. 消去ボタンは表示リストだけを消す。
31. 内部の動画検出リストを削除しない。
32. 表示リストと内部リストを別物として管理する。
33. 表示されていない動画が内部リストに存在する場合、更新時に必要に応じて表示へ復元する。
34. 消去その他のUI操作によって内部リストを空にしない。
35. ダウンロード開始時にボタンをダウンロード中へ変更する。
36. ダウンロード要求を送信しただけでボタンを元へ戻さない。
37. 実際のダウンロード処理が完了するまでボタンをグレーアウトする。
38. 正常完了した場合にダウンロード完了状態へ変更する。
39. 途中でエラー・失敗した場合は、適切な失敗状態へ変更して再度ダウンロード可能にする。
40. 個別ダウンロードと一括ダウンロードで状態管理を分離して破綻させない。
41. 一括ダウンロードでは、ジョブを作成しただけで完了・失敗と判断しない。
42. `queued`は失敗ではない。実際にジョブが待機中なら待機中として扱う。
43. 実際には存在しない古いジョブを現在の`queued`として永続表示しない。
44. CoAppをジョブ管理の責任者とする。
45. Firefox UIとWorkerがそれぞれ勝手にジョブ状態を上書きしない。
46. 一括ダウンロードのジョブ状態をCoAppで一元管理する。
47. Worker Pool方式を使用する。
48. 並列ダウンロード数を設定可能にする。最小1、標準3、最大99とする。
49. 並列数を超えてWorkerを動かさない。
50. Workerが空いたらCoAppがキューから次のジョブを割り当てる。
51. Streamやファイルを使用中のWorkerスロットを次のジョブへ再利用しない。
52. Streamの破棄・ファイル処理などを無秩序なFire-and-Forgetにしない。
53. 「ダウンロード完了」と「後処理完了」を混同しない。
54. 前回発生したファイルロック問題を再発させない。
55. 「別のプロセスで使用されているため、プロセスはファイルにアクセスできません」のような競合を避ける。
56. Worker終了を単純に失敗判定の条件にしない。
57. Coordinator、Worker、ジョブの実際の状態を確認する。
58. Coordinatorが生きているだけでジョブを無条件に成功扱いしない。
59. Coordinatorが生きている待機中ジョブを無条件に失敗扱いしない。
60. Firefox UIの状態とCoApp内部の状態を同期させる。
61. 進捗通知によってダウンロード処理そのものを不必要に遅くしない。
62. ログ・進捗通知を必要以上に大量送信しない。
63. ダウンロード済みファイルを実際の保存先で確認する。
64. ダウンロード済みファイルをユーザーが削除した場合、UI上のダウンロード済み状態も更新する。
65. UIを開き直したときにも保存先の実ファイルを確認する。
66. 「フォルダを開く」は実際の保存先フォルダーを開く。
67. 「動画を再生」は実際に存在する動画ファイルをWindows側へ渡して開く。
68. フォルダーを開く・動画を再生するボタンは表示だけで終わらせず、Native Messaging → CoApp → Windows側の起動処理まで確認する。
69. マウスオーバーによって設定画面の接続状態表示をチラつかせない。
70. PS1実行時にエラーが発生した場合、エラー内容をメッセージボックスで表示する。
71. 同じPS1エラー全文をクリップボードへ文字列として格納する。
72. PS1からCoAppを構築する場合、実際にそのPS1が使用するコンパイラ・参照DLL・.NET環境を前提に実装する。
73. 利用できないC# APIをコードへ入れない。必要な参照が存在しないライブラリを使用しない。
74. 実際にコンパイルできることを確認できていない場合、「コンパイル確認済み」と言わない。
75. 実際に動かせない処理について「動作確認済み」と言わない。
76. 実装前に実際のファイルを確認する。
77. 生成されるC#を確認する。
78. C#のコンパイル経路を確認する。
79. Native Messagingのマニフェストと接続経路を確認する。
80. CoAppの起動経路を確認する。
81. Downloader Workerの起動経路を確認する。
82. ジョブ生成からWorkerへの受け渡しを確認する。
83. ffmpegの起動経路を確認する。
84. ダウンロード開始からデータ取得まで確認する。
85. Stream/FileStreamの終了・破棄まで確認する。
86. Worker終了・再利用まで確認する。
87. 完了判定まで確認する。
88. 完了後のUI反映まで確認する。
89. 一括ダウンロードでは、1件目だけでなく複数件を通したキュー・並列数・状態遷移を確認する。
90. 実際に確認していない工程について、確認したと断言しない。
91. エラーが出た場合は、そのエラーを起点にワークフローを上流から下流まで追う。
92. 症状だけを隠す修正ではなく、状態がどこで作られ、どこで変更され、どこでUIへ渡されるかを確認する。
93. 修正後は、修正箇所だけではなく関連する処理への影響を確認する。
94. 既存機能を壊して新機能を成立させない。
95. ZIP内に勝手なサブフォルダーを追加しない。バージョンフォルダー直下へ必要なファイルを配置する。
96. `Universal-Video-Detector-0.3.53/manifest.json`のようにバージョンフォルダー直下へ配置し、`uvd351`や`video-downloader-extension`のような不要なフォルダーを作らない。
97. 現在のプロジェクト構造を勝手に変更しない。今回追加された仕様も以後の最新版へ引き継ぐ。
98. 「実装した」と言う前に、実際のファイル、生成されるC#、コンパイル経路、Native Messaging、Worker起動、ジョブ生成、ffmpeg起動、Stream終了、完了判定、UI反映まで一連の工程を確認する。
99. プロジェクト内のすべてのテキストファイルはUTF-8 BOMなしで統一する。
100. WindowsのCMD・PowerShellで文字コード依存の構文エラーを発生させない。BATからPowerShellへ処理を渡す場合、CMDによって解釈される特殊文字を考慮する。
101. UTF-8 BOMなしのPowerShellスクリプトをWindows PowerShellで実行する場合、文字コードによって構文が壊れないようにする。BAT・PS1のエラー処理追加自体が新たな構文エラーを発生させないことを確認する。
102. ライブラリに存在するファイルをユーザーへ再提出させない。ライブラリに存在する成果物・過去版・関連ファイルはこちらが取得して使用し、ライブラリに存在しないファイルについてのみ必要に応じて提出を求める。
103. 既存ルールを勝手に変更・上書き・削除しない。ルールの改定・追加・削除は、ユーザーが明示的に宣言した場合、またはこちらが必要性と変更内容を提示しユーザーが承認した場合にのみ行う。
104. ユーザーの承認なしに既存ルールの番号・内容・意味を変更してはならない。
105. バックグラウンド検出と能動的取得を明確に分離する。UIが閉じているイベント監視・検出処理は、サイト上のユーザー操作などで発生した動画情報を内部リストへ登録することを目的とし、自発的なスクロール、ページ送り、追加読み込みなどを開始しない。
106. 能動的なスクロール・追加取得はUI操作を起点とする。検出時に必要情報をまとめて取得し、ダウンロード時に再解析しない。
107. バージョン昇格時はmanifest.json、companion/coapp-version.json、C#ソース内定義、popup.jsなどすべての定数・マニフェストを同一バージョンへ漏れなく更新し、古いバージョン番号をメモリ・ストレージ・通信メッセージへ残さない。
108. FirefoxとCoApp間のNative Messaging JSONの既存フィールド名・データ型を勝手に変更・削除しない。追加フィールドは必ずオプショナルとし、受け手側でundefined/nullを処理する。
109. storage.localの構造を変更する場合は起動時の整合性確認とスキーマ移行を行う。イベントリスナーは二重・三重登録を防止し、Downloader Worker/ffmpegの中断時はtry-finally等で確実にDispose/Killしてプロセスとファイルハンドルを残さない。
110. 大量検出・表示はsendMessageやDOM描画を一度に集中させず、requestAnimationFrame/requestIdleCallback等で分散する。AI・開発者は仮説と実績を明確に分離し、開発終了後はAから始まるユーザー確認項目を提示し、F/P/?/Hを記録する。問題Aから開始し、重大な未確認事項が残る場合は完成扱いにしない。

## 保存フォルダー仕様・責任分離

保存先からサイト命名を行い、その中に動画を保存する。サイト名＋サイトタイトル、または連番で命名されるページ単位フォルダーへ保存し、サイト名だけの親フォルダーや不要な中間階層を作らない。連番方式は動画ファイル名ではなく保存フォルダー名に適用する。動画ファイル命名はフォルダー命名と独立して管理する。同一ページの複数動画は同じページ対応フォルダーへ保存し、同じページへ後日戻った場合は既存フォルダーを再利用する。同一サイト内の別ページは別ページとして管理する。

ページ識別と動画識別は分離する。URLだけをページ識別の根拠にせず、時間を空けても基本的に変化しないページ固有情報を複数組み合わせる。UIは検出時にページ識別・動画識別に必要な情報を収集・選別して内部リストへ保持し、ダウンロード時に再解析しない。CoAppはページ同一性、動画同一性、保存先、連番、既存ファイル、ジョブ状態、ダウンロード可否を最終判断する。UIはその判断を独自に最終確定しない。

UIは表示上の状態を勝手に最終確定せず、BackgroundもCoAppのジョブ状態を上書きせず、Workerも自身の終了だけを根拠に最終状態を決定しない。CoAppがWorkerの実処理、実ファイル、ffmpeg終了、Stream/FileStream終了、後処理結果を総合して完了・失敗を判断する。UI再オープン時はCoAppの現在状態と実ファイルの存在から表示を再構築する。UIの閉鎖・消去・再描画・更新を理由にCoApp管理情報を削除しない。

## 開発工程の絶対条件

開発前に全開発ルール、現行最新版、実際のファイル・構造・依存関係・実装経路・影響範囲を確認する。開発中は現行最新版だけを継続更新し、実際のコードと処理経路を追跡し、未確認事項を推測で確定扱いしない。開発後は全ルールに照らして、バージョン同期、文字コード、Native Messaging、CoApp、Worker、ジョブ、ffmpeg、Stream、完了判定、UI反映、既存機能への副作用、エラー経路、状態遷移、依存関係、成果物構造を再確認する。

最重要ワークフローは、実際のファイル → 実際のコード → コンパイル経路 → Native Messaging → CoApp → Worker → ジョブ → ffmpeg → Stream → 実ファイル → 完了判定 → UI → フォルダーを開く → 動画再生まで追跡する。実際に確認していないものを「確認済み」「動作済み」とは言わない。

## 111以降

既存の正式な111以降のルールは、`UVD-Development-Rules-111-Addendum.md` の内容をそのまま継承する。

### 111. ルール追加権限と既存ルール保護
開発中に再発防止、設計の明確化、検証漏れ防止、状態管理の安全性向上などのため新たなルールが必要になった場合、開発側はユーザーへの事前確認なしに新規ルールを追加してよい。ただし、既存ルールの削除、内容変更、意味変更、番号変更、統合は原則としてユーザーの明示的な承認なしに行ってはならない。ただし、既存ルールに既に同趣旨の規定が存在し、その当該ルールへの加筆・修正・統合だけで対応できる場合は、ユーザーが既に承認したルール編集権限に基づき、追加承認なしで当該ルールを編集してよい。この場合、編集後の最終的なルール内容をユーザーへ共有する。

### 112. UIとCoAppの責任境界
UIは情報の収集、必要情報の選別・保持、CoAppへの要求送信、CoAppから返された結果の表示を主責務とする。ダウンロード可否、重複判定、既存ファイル判定、ページ識別、保存フォルダー決定、連番割り当て、ジョブ生成、ジョブ状態管理、Worker割り当て、実ダウンロード、完了判定はCoAppを最終的な判断主体とする。UI側でCoAppと同じ判断を二重に実装しない。UIが先回りしてダウンロードを拒否したり、CoAppへ渡す前に成功・失敗・重複を確定したりしてはならない。UIはCoAppへの要求を送信しただけで処理完了と扱わない。ただし、UIの表示状態をCoAppの状態より優先して書き換えてはならない。

### 113. 検出時の情報収集とダウンロード時の即時送信
動画検出時に後でCoAppが判断するために必要となる情報をまとめて収集し、収集後は不要な情報を除外して内部リストへ必要最小限を保持する。ダウンロード要求時にページを再解析して情報を集め直すことを原則禁止し、既に内部リストへ保存した情報をそのままCoAppへ送信する。例外が必要なら実装前に必要性、取得項目、発生条件、内部リストへの影響を確認する。

### 114. ページ識別情報の多段管理
ページ単位の識別ではURLだけを唯一の識別子として使用しない。UIは利用可能な範囲でSchema.org/JSON-LDの@id、identifier、mainEntityOfPage、Microdataのitemid、canonical URL、og:urlなどを検出時に収集する。存在しない情報を生成してはならない。CoAppは複数の識別情報を比較し、十分な根拠がある場合だけ既存ページとして認識する。タイトル単独、ホスト単独、一般的なサイト名単独、動画ファイル名単独をページ識別の確定根拠にしない。

### 115. CoAppによる識別情報の永続記憶
CoAppはページ識別に使用した情報を永続的な対応情報として保存し、後日の再アクセス時にも過去に認識したページと照合できるようにする。URL、canonical URL、og:urlなどが変化しても強い識別情報との対応で既存ページを認識する設計を優先する。一つの弱い情報を根拠に複数ページを誤って同一ページへ統合しない。

### 116. 連番フォルダーの対応管理
連番方式は現在の最大番号だけでなく、ページ識別情報とフォルダー番号の対応をCoApp側で永続管理する。同一ページが後日再度ダウンロードされた場合は既存連番フォルダーを再利用し、安全に一致しない場合だけ新しい連番を割り当てる。同じサイトの別ページは別ページとして扱う。旧方式のサイト＋タイトルだけの対応表を新方式のページ同一性確定根拠にしない。

### 117. フォルダー構造の固定仕様
保存先はユーザー指定の保存先を起点とし、その直下に設定された命名方式に従うページ単位フォルダーを作成して動画を保存する。サイト名だけの親フォルダー、不要な中間フォルダー、サイト別の追加階層、UI都合の一時フォルダーを勝手に追加しない。ページ単位フォルダーはサイト名＋ページタイトルまたはCoApp管理の連番とし、動画ファイル名とは独立させる。

### 118. 識別情報と動画識別情報の分離
どのページに属するかと、どの動画そのものかを別情報として管理する。ページ識別情報だけで異なる動画を同一動画として扱わない。動画重複判定ではmedia URL、動画識別情報、ファイル名、サムネイル、時間、サイズなど利用可能な複数要素を組み合わせる。同じページの複数動画は独立した動画項目として扱う。

### 119. UIの情報選別基準
UIは取得可能な情報を無制限にCoAppへ送信しない。検出時に収集した情報からCoAppのページ識別、動画識別、ダウンロード、保存、状態管理に必要な情報を選別して内部リストへ保持する。選別は検出時に行い、ダウンロード要求時の待ち時間を増やさない。識別精度や互換性に必要な情報を削除する場合は影響を確認する。

### 120. 全体フロー検証の必須化
新機能または修正では変更箇所だけを確認して終わらない。UI情報収集 → 内部リスト登録 → 内部リスト保持 → ダウンロード要求 → Native Messaging → CoApp受信 → 入力検証 → ページ識別 → 動画重複判定 → 保存フォルダー決定 → ジョブ生成 → Worker割り当て → ffmpegまたは直接ダウンロード → Stream/FileStream処理 → 後処理 → 実ファイル確認 → ジョブ完了判定 → CoApp状態保存 → Native Messaging返却 → UI状態反映を追跡する。フォルダーを開く、動画を再生する、既存ファイルをスキップする、UIを閉じて再度開く、一括ダウンロードする場合も実状態とUI表示が一致するまで追跡する。

### 121. 静的確認と動的確認の分離
静的解析で確認できる内容と実機でしか確認できない内容を明確に分離する。ソースコード上の整合性、参照関係、JSON構造、文字コード、バージョン同期などを確認しただけで、Native Messaging、実CoApp起動、実Worker起動、ffmpeg実行、実ファイル生成、Firefox UI反映まで動作確認済みとは扱わない。実機確認できない環境ではその範囲を未確認として記録する。

### 122. 修正前後の影響確認
一つの修正が別機能の状態管理や通信に影響する可能性を必ず確認する。ページ識別情報を追加・変更した場合は、内部リストの重複統合、更新ボタン、消去ボタン、UI再オープン、一括ダウンロード、既存ファイル判定、連番フォルダー再利用、ジョブ状態表示への影響を確認する。UI簡素化でもCoAppに必要な情報が欠落しないことを確認する。

### 123. 状態の単一責任
同一状態についてUI、Background、CoApp、Workerがそれぞれ独立して最終判断を保持しない。ダウンロードジョブの状態、ページ識別と連番フォルダーの対応、実ファイルの存在状態について最終的な判断主体を明確にする。UIは表示用の状態を持てるがCoAppの確定状態を無断で上書きしない。

### 124. エラー伝播の完全性
下流で発生したエラーをUI側で握り潰して成功に変換しない。CoApp、Worker、ffmpeg、ファイル処理、Native Messagingの各段階のエラーは原因を失わずCoAppの状態へ反映し、必要ならUIへ返す。正常な非同期処理中のqueued、starting、downloadingをエラーとして扱わない。

### 125. テスト前の成果物固定
ユーザーが前バージョンのテストを完了していなくても、新しい修正はその時点の現行最新版を唯一のベースとする。ユーザーのテスト状況を理由に過去版へ戻さない。新しい成果物を作る場合は変更内容、影響範囲、未確認事項を実装目録へ記録する。

### 126. 開発終了判定
「実装完了」はコードを書き終えたことを意味しない。実装後に全ルールとの整合性、バージョン同期、文字コード、通信、状態遷移、依存関係、副作用、成果物構造、実行経路を再確認する。確認できない工程が残っている場合、その工程について確認済み・動作済みと断言しない。重大な未確認事項や既知の不具合が残る場合は実装完了扱いにせず未確認または保留として記録する。

## 開発時の追加遵守事項

新規ルールが必要と認識した場合は、既存ルールを変更せず、新規ルール案として番号、目的、具体的内容、既存ルールへの影響をユーザーへ事前に提示する。ユーザーが承認するまで正式登録しない。既存ルールとの競合がある場合は既存ルールを無断変更せず、競合内容と変更案を提示する。

実装・修正・動作確認の報告は、実際に確認できた範囲に限定する。コンパイル不能、Native Messaging接続不能、CoApp起動不能、Worker処理不能、ffmpeg起動不能、実ファイル生成不能、完了判定不能、UI反映不能など重大な未確認事項が残る場合、成果物を完成品として扱わない。

ユーザーによる確認結果は問題Aから開始し、各問題をF=Failed、P=Passed、?=Question、H=Holdで記録する。Fまたは?は未解決・未確定として必要な調査・修正・再確認を行い、Hは未確認として後続バージョンへ引き継ぎ、Pの場合のみ今回の実動作確認をPassedとして記録する。

## 127以降：承認済み追加ルール

### 127. 開発終了後のユーザー確認
開発終了後に成果物をユーザーへ渡す際、今回の修正と確認動作を依頼する。動作確認は問題Aから始め、1つの問題につきアルファベット1文字を割り当てる。ユーザーはF=Failed（失敗）、P=Passed（今回はこの動作で問題なし）、?=Question（期待通りの働きか不明）、H=Hold（今回はその問題の確認はできないので次以降に繰り越し）として回答する。

### 128. 開発前の全ルール確認
開発前に全開発ルールを確認する。

### 129. 開発前の最新版取得
開発前にライブラリから現在の最新版を取得する。

### 130. 開発前の実体・影響範囲確認
開発前に実際のファイル・構造・依存関係・実装経路・影響範囲を確認する。

### 131. 現行最新版の継続更新
開発中は既存ルールを維持し、現行最新版だけを継続更新する。

### 132. 未確認事項の扱い
未確認事項を推測で確定扱いせず、実際のコードと処理経路を追跡する。

### 133. 開発後の全ルール再評価
開発後は全ルールに照らして実装を再評価する。

### 134. 開発後の主要工程確認
開発後はバージョン同期、文字コード、Native Messaging、CoApp、Worker、ジョブ、ffmpeg、Stream、完了判定、UI反映まで確認する。

### 135. 副作用・状態遷移・依存関係確認
開発後は既存機能への副作用・状態遷移・依存関係を確認する。

### 136. 確認範囲の明示
実際に確認できた範囲と確認できていない範囲を明示する。

### 137. 問題が残る場合の完了禁止
問題があれば「実装完了」と扱わない。

### 138. ユーザー側確認の記録
ユーザー側に今回の修正と確認動作を依頼する。動作確認は問題Aから始め、1つの問題につきアルファベット1文字を割り当てる。

### 139. 最重要ワークフローの完全追跡
最重要ワークフローとして、実際のファイル → 実際のコード → コンパイル経路 → Native Messaging → CoApp → Worker → ジョブ → ffmpeg → Stream → 完了判定 → UI → フォルダーを開く → 動画再生まで追い、確認していないものを「確認済み」「動作済み」とは言わない。

### 140. 細部までの適合性確認
細かいフローまで追い、動作が予定通りのものになっているか確認し、ルールに則っているか確認する。

### 141. UIとCoAppの責任分離
UIは情報収集・必要情報の選別・内部リスト保持・CoAppへの送信・CoAppからの結果表示だけを担当する。ページ識別、動画重複判定、保存フォルダー決定、連番管理、ジョブ管理、ダウンロード判断はCoAppが最終責任を持つ。

### 142. 新規ルール追加原則
新規ルールが必要な場合、理由と影響範囲を提示しユーザーの承認を得て末尾に追記する。ただし、既存ルールに既に同趣旨の規定が存在し、その当該ルールへの加筆・修正・統合だけで対応できる場合は、ユーザーが既に承認したルール編集権限に基づき、追加承認なしで当該ルールを編集してよい。この場合、編集後の最終的なルール内容をユーザーへ共有する。それ以外の既存ルールについては、ユーザーの明示的承認なしに無断改変・削除・意味変更・番号変更・統合を行わない。

### 143. 標準開発・検証実行シーケンス
すべての開発・修正作業において、以下の実行順序を標準フローとして確実に厳守する。
1. 事前準備・解析：現在の最新版をライブラリから取得、全開発ルールを確認、実ファイル・プロジェクト構造・依存関係の全容を確認、影響範囲を事前確認する。
2. 実装・一次検証：最新版をベースに実装し、C# / PS1 / TS / JSのコンパイル・構文エラー、実装漏れ、型不一致等を確認する。
3. エンドツーエンド処理経路追跡：Native Messaging接続・通信フォーマット、CoApp起動・ハンドリング、Worker割り当て・並列実行、queued / running / completed / failed管理、ffmpeg呼び出し・引数・終了コード、Stream / FileStream生成・書き込み・Dispose、実ファイル生成・出力先、完了判定、UI状態反映・描画分散、「フォルダーを開く」、動画再生まで確認する。
4. 最終適合性・安全性再評価：副作用、状態遷移、依存関係、全開発ルールへの適合性を再確認し、問題A〜のユーザー確認項目と未検証範囲を整理する。



### 【保存フォルダー・ページ識別・責任分離の明確化】

### 144. 保存フォルダーの最上位構造を固定する。
保存先はユーザーが指定した保存先を起点とし、その直下に設定された命名方式によるページ単位フォルダーを作成する。サイト名だけの親フォルダー、サイト別の中間階層、UI都合の一時フォルダーなどを、既存仕様にない形で追加してはならない。

### 145. ページフォルダーと動画ファイルの命名責任を完全に分離する。
ページフォルダーの命名方式と動画ファイルの命名方式を独立して管理し、連番方式を動画ファイル名へ誤って適用してはならない。

### 146. 同一ページに属する複数動画は同一ページフォルダーへ保存する。
同一ページから複数の動画が検出された場合、それぞれを別動画として管理しながら、保存先はそのページに割り当てられた同一フォルダーとする。

### 147. ページフォルダーの再利用はページ同一性の確定後に行う。
後日の再アクセス時に既存フォルダーを再利用する場合、単一の弱い識別情報ではなく、CoAppが管理するページ識別情報との照合結果に基づいて再利用を決定する。

### 148. 旧方式のフォルダー名だけをページ同一性の根拠にしない。
過去に作成されたサイト名＋タイトル方式などのフォルダー名やフォルダー番号だけから、新方式におけるページ同一性を推測して確定してはならない。

### 【ページ識別情報の収集・照合】

### 149. ページ識別情報と動画識別情報を別のデータとして管理する。
ページを識別する情報を動画そのものの識別情報として流用してはならず、両者の生成、保存、比較、更新を独立して扱う。

### 150. 利用可能な強いページ識別情報を優先する。
検出時に利用可能であれば、Schema.org/JSON-LDの`@id`、`identifier`、`mainEntityOfPage`、Microdataの`itemid`、canonical URL、`og:url`などを取得対象とし、存在しない情報を推測して生成してはならない。

### 151. ページ識別情報の強度を考慮して照合する。
CoAppは識別情報ごとの信頼性を考慮し、タイトル、ホスト名、サイト名、動画ファイル名などの単独情報だけでページ同一性を確定してはならない。

### 152. URL変更だけを理由に既存ページを別ページとして扱わない。
canonical URL、`og:url`、ページ固有IDなどの強い識別情報が一致または十分な関連性を示す場合、通常URLの変更だけで別ページとして新規フォルダーを割り当てない。

### 153. 識別情報の不一致を無理に統合しない。
複数の識別情報が異なるページを示している場合、フォルダー再利用やページ統合を強制せず、必要な情報を追加確認したうえで判断する。

### 154. ページ識別情報を新規生成して同一性を補完してはならない。
ページ上に存在しない識別子を、タイトルやURLなどから独自に作成して、それを外部由来の正式なページ識別情報として扱ってはならない。内部管理用IDが必要な場合は、外部識別情報とは明確に区別する。

### 【動画識別・重複判定】

### 155. 動画重複判定はページ同一性とは独立して行う。
同一ページだから同一動画、別ページだから別動画、と自動的に決めてはならない。

### 156. 動画重複判定では利用可能な複数情報を使用する。
media URL、動画固有識別情報、ファイル名、サムネイル、動画時間、サイズその他利用可能な情報を組み合わせ、タイトル単独やファイル名単独による誤判定を防止する。

### 157. 同一ページ内の複数動画を誤って統合しない。
同じページから複数の動画が検出された場合、それぞれを独立した動画項目として保持し、ページ識別情報だけを理由に一つへ統合してはならない。

### 【データ所有者・責任境界】

### 158. 各データの最終責任主体を明確にする。
UIは収集・選別・保持・送信・表示、CoAppは識別・保存先・ジョブ・ダウンロード可否・完了判定などの最終判断、Workerは割り当てられた実処理を担当する。複数層が同一データについて独立して最終判断を行わない。

### 159. UIはCoAppの判断結果を推測して先行確定しない。
CoAppへの要求送信前後に、UIが独自に「成功」「失敗」「重複」「完了」「ダウンロード済み」などの最終状態を確定してはならない。

### 160. Workerは実処理の結果をCoAppへ正確に報告する。
Workerは自身の終了だけを成功・失敗の根拠とせず、処理結果、エラー情報、出力結果などをCoAppが最終判定できる形で報告する。

### 【状態遷移】

### 161. ジョブ状態には許可された状態遷移を設ける。
`queued`、`running`、`completed`、`failed`などの状態について、実際の処理順序と矛盾する状態変更を行ってはならない。ジョブ生成直後に完了扱いするなど、実処理を経由しない状態遷移を禁止する。

### 162. 状態変更には変更根拠を持たせる。
ジョブ状態を変更する場合、その変更がどの実処理、通信結果、Worker結果、ファイル状態、ffmpeg結果、後処理結果などを根拠としているか追跡可能にする。

### 163. 非同期処理中の一時状態を失敗として扱わない。
`queued`、`starting`、`running`、ダウンロード中など、正常な処理過程に存在する状態を、単に時間が経過したことやWorkerから最終結果がまだ返っていないことだけを理由に失敗扱いしない。

### 164. 状態を逆戻りさせる場合は明確な理由を必要とする。
`completed`から`running`へ戻すなど、通常の処理順序に反する状態変更をUIやBackgroundが独自判断で行ってはならない。実ファイル削除などによって再評価が必要な場合は、CoAppが実状態を確認して判断する。

### 【異常終了・再起動・状態復元】

### 165. CoApp異常終了時のジョブ状態を次回起動時に再検証する。
永続化された`queued`や`running`などの状態を、次回起動時に無条件で現在状態として復元してはならない。実際のWorker、プロセス、ジョブ、実ファイルを確認して状態を再構築する。

### 166. Firefox再起動時に表示状態だけからジョブ状態を復元しない。
Firefox UIやstorage.localに残った表示情報をCoAppの最終状態として扱わず、CoAppから現在状態を取得して再構築する。

### 167. 異常終了したWorkerを無条件に失敗扱いしない。
Worker終了時には、実ファイル、ffmpeg、Stream/FileStream、後処理、CoApp側のジョブ情報などを確認し、実際の結果を総合して状態を判断する。

### 168. 再起動時に存在しないジョブを復活させない。
永続化データだけを根拠として、実際には存在しない古いジョブを現在実行中または待機中として表示・処理してはならない。

### 【同時実行・二重要求】

### 169. 同一動画に対する重複ジョブ生成を防止する。
同じ動画について短時間に複数のダウンロード要求が発生した場合、CoAppは既存ジョブの状態を確認し、不要な二重ジョブを生成しない。

### 170. 同一ファイルへの同時書き込みを防止する。
複数Workerや再試行処理によって同一出力ファイルへ同時に書き込む状態を作らない。

### 171. 同一ページのフォルダー割り当てを競合させない。
複数の検出・ダウンロード要求が同時に発生した場合でも、同一ページへ異なる連番フォルダーを重複割り当てしない。

### 172. Worker Poolのスロット所有権を明確にする。
一つのWorkerスロットについて、前ジョブのStream、ファイル、プロセス、後処理が完全に終了する前に次ジョブを割り当てない。

### 【ファイル・保存処理】

### 173. 不完全な出力ファイルを完了ファイルとして扱わない。
ダウンロード途中、ffmpeg異常終了、書き込みエラー、Stream終了失敗などによって生成された不完全ファイルを、正常なダウンロード済みファイルとして扱ってはならない。

### 174. 完了判定とファイル存在判定を分離する。
ファイルが存在することだけを理由に正常完了とせず、CoAppがダウンロード処理、ffmpeg終了、Stream/FileStream終了、後処理などを総合して完了を判断する。

### 175. 再試行時に不完全ファイルを誤って完成品として再利用しない。
失敗したダウンロードを再実行する場合、前回の不完全ファイルを正常ファイルと誤認しないようにする。

### 176. 出力ファイルの競合を防止する。
同名ファイルが存在する場合の扱いを明確にし、別Workerや別ジョブが同じファイルを上書きする状態を作らない。

### 【Windows保存パス・ファイル名】

### 177. Windowsで使用できない文字を適切に処理する。
ページタイトル、サイト名、動画タイトルなどを保存フォルダー名やファイル名へ使用する場合、Windowsで使用できない文字や予約名を適切に処理し、無効なパスを生成しない。

### 178. パス長・空白・ピリオド等を考慮する。
Windowsのパス・ファイル名として問題となる長さ、末尾の空白、末尾のピリオドその他の制約を考慮し、保存処理が失敗しないようにする。

### 179. ファイル名の安全化によって識別情報を破壊しない。
Windows向けにタイトルやファイル名を安全化する処理と、ページ識別・動画識別情報を混同しない。表示名を変更しても識別情報そのものを変更したことにしてはならない。

### 【Native Messaging・通信エラー】

### 180. 通信失敗とジョブ失敗を区別する。
Native Messagingなどの通信が一時的に失敗した場合、それだけを理由に実際のダウンロード処理を失敗と確定してはならない。CoApp側の実状態を確認したうえで判断する。

### 181. 通信途中で状態を失わない。
UIとCoApp間の通信が途切れた場合でも、CoAppが管理しているジョブ、ページ識別、動画識別、保存先などの状態を失わせない。

### 182. 通信メッセージのエラーを握り潰さない。
JSON解析失敗、必須情報欠落、型不一致、未知の状態などを正常処理として扱わず、原因を追跡可能な形で処理する。

### 183. 再送による二重処理を防止する。
通信障害などによって同一要求が再送された場合、CoAppは要求の重複を確認し、同一ジョブを不必要に複数生成しない。

### 【エラー伝播・原因追跡】

### 184. 下流エラーを上流で成功へ変換しない。
Worker、ffmpeg、Stream/FileStream、ファイル処理、CoApp、Native Messagingなどの下流で発生したエラーを、UIや別層が勝手に成功状態へ変換してはならない。

### 185. エラーの発生位置と原因を追跡可能にする。
エラーが発生した場合、単にエラーメッセージを表示するだけでなく、どの処理、どの状態、どの入力、どの依存関係によって発生したかを上流から下流まで確認する。

### 186. エラーを症状だけ隠す修正を禁止する。
画面上のエラー表示を消す、例外を握り潰す、失敗を成功として表示するなど、根本原因を残したまま症状だけを隠す修正を行わない。

### 187. エラー修正後は同じ原因が別経路に残っていないか確認する。
一つのエラーを修正した場合、同じ変数、状態、通信形式、依存関係、処理経路が別の箇所にも存在しないか確認する。

### 【データ整合性・所有権】

### 188. データの生成元と変更主体を明確にする。
各データについて、どの層が生成し、どの層が変更でき、どの層が最終判断するかを明確にし、複数層が無秩序に同じデータを書き換えない。

### 189. UI表示データからCoApp管理データへ逆同期しない。
表示リスト、表示状態、DOMの内容などをCoApp管理情報の根拠として扱わない。UIの表示内容が空になった、削除された、再描画されたことを理由に内部管理情報を削除・上書きしてはならない。

### 190. 永続データと実状態の不一致を検出する。
storage.local、CoAppの永続データ、ジョブ情報、ページ対応情報などと実際のファイル・プロセス・Worker状態が一致しない場合、その不一致を検出して安全に再評価する。

### 【回帰防止】

### 191. 修正前に影響を受ける既存機能を特定する。
新しい機能や修正を行う前に、変更対象が既存の検出、内部リスト、更新、消去、ダウンロード、一括処理、設定、保存フォルダー、再生、フォルダー表示などへ与える影響を確認する。

### 192. 修正後に変更対象以外の正常機能も確認する。
修正箇所だけが正常であることをもって完了とせず、変更によって影響を受ける可能性のある既存機能について回帰確認を行う。

### 193. 正常動作を新機能の成立条件として犠牲にしない。
新しい仕様を成立させるために、既存の正常な検出、自動読み込み、内部リスト保持、設定保持、ダウンロード状態管理その他の既存仕様を意図せず破壊してはならない。

### 【実装目録・変更管理】

### 194. 各修正について変更内容と影響範囲を記録する。
修正したファイル、変更した処理、変更理由、依存関係、影響範囲、確認した項目、未確認項目を追跡可能な形で管理する。

### 195. 修正による副作用を変更単位で確認する。
一つの修正が複数の処理経路に影響する場合、直接変更した箇所だけでなく、その変更値や状態を利用する関連処理まで確認する。

### 196. 未確認事項を実績として記録しない。
ソースコードを読んだだけの確認、静的解析だけの確認、理論上の確認と、実際に実行して確認した結果を明確に分離する。

### 【静的確認・動的確認】

### 197. 静的確認と動的確認を明確に区別する。
コード、参照、JSON、文字コード、バージョン番号、構造などを確認しただけでは、実際のNative Messaging、CoApp、Worker、ffmpeg、実ファイル生成、UI描画が動作したとは扱わない。

### 198. 実機確認できない工程は未確認として扱う。
開発環境から実際のFirefox、Windows、Native Messaging、CoApp、Worker、ffmpeg、保存先などを実行確認できない場合、その工程について「動作確認済み」と報告しない。

### 199. コンパイル成功と実動作成功を別々に扱う。
C#やJavaScriptなどのコンパイル・構文確認に成功しても、実行経路全体が正常であることを意味しない。コンパイル確認と実動作確認を別の結果として記録する。

### 【開発終了判定の強化】

### 200. 「実装完了」「コンパイル確認済み」「動作確認済み」を別の状態として扱う。
コード実装、コンパイル成功、実環境での動作確認を同一の意味として報告してはならない。

### 201. 重大な未確認工程がある場合は完成品として扱わない。
Native Messaging、CoApp、Worker、ffmpeg、実ファイル生成、完了判定、UI反映など、今回の変更に関係する重大な工程が未確認の場合、成果物を完全な完成品として扱わない。

### 202. 開発終了時に実装・検証・未確認事項を分離して報告する。
終了報告では「実装した内容」「実際に確認できた内容」「確認できなかった内容」「既知の問題」「ユーザー側で確認が必要な項目」を混同しない。

### 203. ユーザー確認でPになるまで実動作確認を完了扱いにしない。
ユーザー側でしか確認できない実環境の動作については、問題A〜の確認結果がPになるまで、その確認項目を今回の実動作確認済みとして確定しない。

### 【将来のルール追加】

### 204. 新規ルールは既存ルールとの重複・矛盾を確認してから追加する。
新規ルールを正式登録する場合、既存ルールで既に定義されている内容との重複、既存ルールとの矛盾、実装への影響範囲を確認する。

### 205. 既存ルールとの競合は新規ルール側で解決できるか先に検討する。
新規仕様と既存ルールが競合する場合、既存ルールを無断変更・削除・統合せず、新規ルール側で適用範囲や例外条件を明確化できるかを先に検討する。

### 206. ルール番号と履歴を維持する。
正式登録されたルールについて、後から追加・変更された場合でも過去の番号や変更履歴を勝手に再採番せず、変更が承認された場合は変更内容を追跡可能にする。

## 【広告・フィルターリスト連携】

### 207. 公開広告・トラッキングフィルターリストを広告確度の証拠として利用する。
UVDは公開フィルターリストを広告動画判定の入力として利用できる。ただし、フィルター一致だけを広告確定条件にはしない。

### 208. 初期対象リストをEasyList、EasyPrivacy、uBlock filtersに限定する。
EasyListを主要な広告証拠、EasyPrivacyを補助的なトラッキング証拠、uBlock filtersを重要な広告・動画広告証拠として扱う。EasyPrivacy単独ではHIGHにしない。明示的な追加仕様なしに他のリストを追加しない。

### 209. 他の広告ブロッカー拡張へ依存しない。
インストール済みuBlock Origin等の状態、API、ブロック結果、内部データベースをUVDの判定根拠にしない。UVD自身が対象リストを取得・保持・解析する。

### 210. フィルター一致結果と広告確度を分離する。
広告確度はHIGH、MEDIUM、LOW、NORMALの4状態とし、UI上は「広告確度 HIGH/MEDIUM/LOW/通常動画」として扱う。

### 211. HIGHは複数の強い証拠がある場合だけ付与する。
単一の弱い証拠だけでHIGHにしてはならない。

### 212. MEDIUMは広告らしいがHIGHに不足する場合に付与する。

### 213. LOWは広告証拠があるが誤判定リスクが残る場合に付与し、内部には保持する。

### 214. NORMALは広告とする十分な証拠がない場合に付与し、フィルター強度に関係なく表示対象とする。

### 215. 広告フィルター強度を弱・中・強から設定できるようにする。
強度は広告確度そのものを変更せず、表示フィルタリングだけを変更する。

### 216. 強度による表示範囲を固定する。
弱はHIGHのみ、中はHIGH+MEDIUM、強はHIGH+MEDIUM+LOWを非表示とし、NORMALは常に表示する。

### 217. 広告フィルターで非表示になった動画を内部検出リストから削除しない。
UI表示は内部リストと現在の強度から導出し、強度変更だけで再検出しない。

### 218. 広告判定は既存動画検出から分離する。
既存の検出、遅延検出、Adapterを置換・削除せず、広告判定失敗で検出結果を失わせない。

### 219. EasyList/uBOの対応構文だけを明示的に解析する。
未対応の拡張・特殊構文は推測せず、広告ではないという証拠にもせず、判定入力から無視する。

### 220. フィルターリストは更新可能なデータとして管理する。
リスト名、取得元、最終成功取得、更新識別情報、現在状態、失敗状態等を保持し、リスト側の更新メタデータを可能な限り尊重する。

### 221. フィルターリスト更新失敗を成功扱いしない。
新しいリストの取得・解析・検証に失敗した場合、最後に有効だったリストを保持する。一度も取得できていない場合でも動画検出自体は継続し、広告フィルタリングだけをスキップできる。

### 222. フィルターリストのライセンス・帰属表示を行う。
使用するリストの出典、ライセンス、目的をUVDの配布物・設定等で確認できるようにする。

### 223. 取得元を公式または指定された信頼できる提供元に固定し、取得先定義を集中管理する。
第三者ミラーを追加する場合は、同一性、信頼性、更新性、利用条件を確認する。

### 224. 広告確度の根拠を追跡可能にする。
使用リスト、可能な限りルール種別・識別情報、UVD側の証拠、最終広告確度を内部的に追跡できるようにする。

### 225. 誤判定防止を優先する。
証拠が不足する場合はHIGH/MEDIUM/LOWを強制せずNORMALとして扱う。

### 226. 広告フィルターをダウンロードエンジンの変更に使わない。
広告動画の非表示はUI上の表示・選択制御であり、内部検出情報やダウンロード処理そのものを破壊しない。

### 227. 強度変更は既存の広告確度から再描画する。
強度変更だけでページ再読み込み、動画再検出、CoApp再送信を行わない。

### 228. 将来の追加リストは個別評価する。
目的、既存リストとの重複、ライセンス、構文、誤判定リスクを評価し、単に「uBOのデフォルトリストだから」という理由だけで追加しない。

### 229. 広告フィルター機能の失敗で既存機能を停止させない。
リスト取得・解析・分類・更新の失敗が動画検出、動画一覧、ダウンロード、再生、履歴等を停止させない。



## 【性能最適化・Phase 1～3】

### 230. 検出能力を維持した入力軽量化
動画検出の検出経路そのものを削除・無効化して性能改善を行ってはならない。性能改善は、動画と無関係な入力の早期除外、対象限定、重複処理排除、バッチ化、キャッシュ、解析予算等によって行うことを基本とする。

### 231. fetch/XHR本文の無条件読み取りを禁止する
fetch/XHR等のレスポンス本文を無条件にclone、text、responseText等で読み取ってはならない。本文解析前にURL、Content-Type、responseType、サイズ等を確認し、解析不要なレスポンスは本文を読み取らず終了する。

### 232. 明らかなバイナリ・メディア本文を解析しない
明らかな動画、音声、画像、フォント、WASM、バイナリ、動画・音声セグメント等について、動画検出用のテキスト本文解析を行わない。拡張子だけを唯一の判定材料としてはならず、複数の情報を組み合わせる。

### 233. fetch/XHR検出経路を維持する
fetch/XHR等による動画情報検出経路を削除・無効化してはならない。本文読み取りを抑制しても、監視・検出能力は維持する。

### 234. MutationObserverの重複監視を防止する
同一Documentに対する重複監視・重複処理を原則として発生させない。可能な場合は中央Observerで変更を集約し、必要な検出系へ配布する。複数Observerが必要な場合は監視範囲と責務を明確に分離する。既存Adapterを削除してはならない。

### 235. Mutation変更Nodeを重複排除する
同一Mutation batch内で同一Nodeまたは同一対象を複数回解析しない。Set等で重複排除し、可能な限りバッチ単位で処理する。

### 236. Mutation最適化後も受動検出を維持する
MutationObserverの最適化によって受動検出と能動取得の分離を変更してはならない。受動処理から自発的なスクロール、ページ送り、追加読み込み、検出目的の追加通信を開始してはならない。

### 237. 固定タイマーによる全画像再走査を禁止する
受動検出で固定タイマーを用いてdocument.images等の全Document画像走査を繰り返してはならない。初期処理は必要な範囲で一度だけ行い、以後は変更された対象を処理する。

### 238. 画像・カード解析は変更対象へ限定する
画像由来の動画候補を扱う場合、変更されたimg等から関連する親要素・カード等へ対象範囲を限定する。全Document再走査によって代替してはならない。ただし既存検出能力を低下させない。

### 239. 巨大レスポンスは段階的に解析する
巨大なfetch/XHRレスポンスに対して無条件にJSON.parseや全体再帰解析を行わない。軽量な候補判定を先に行う。ただしEarly Exitだけを理由に動画検出経路を完全に破棄してはならず、候補が確認された場合は定められた解析予算内で追加解析できる構造とする。軽量判定を「動画が存在しない」ことの証明として扱わない。

### 240. 重量解析に予算を設ける
JSON等の重量データを解析する場合、サイズ、ノード数、深度、処理時間等について適切な予算または上限を設け、1回の処理がFirefoxのイベントループを長時間占有しないようにする。

### 241. 軽量候補判定と重量解析を分離する
検出処理は軽量候補判定、動画候補化、詳細解析、Video Candidate化、既存の多重シグナル識別等の段階に分離できる。動画候補になる可能性が低い入力へ重量解析を先に実行してはならない。

### 242. 重複判定の精度を維持した候補Index化
media URL、video ID、filename、thumbnail等の安定した識別情報についてIndexやMapを利用して候補を絞り込める。ただし最終判定では既存の多重シグナルによる重複判定を維持し、Indexだけで重複確定してはならない。

### 243. 広告分類を動画検出から分離する
広告判定が動画検出そのものを不必要に遅延させないよう、動画検出・内部状態登録と広告分類・表示分類を可能な範囲で分離する。広告判定失敗によって内部検出結果を失わせてはならない。

### 244. 性能改善は段階実装し、検証後に次Phaseへ進む
今回の性能改善は、新規性能改善計画のPhase 1～3として段階的に実施する。Phase 1はfetch/XHR本文読み取り抑制、MutationObserver重複監視解消、Mutation batch重複排除、固定タイマーによる全画像再走査廃止を対象とする。Phase 2はPhase 1の検証完了後に巨大JSON Early Exit、解析予算、DOM解析予算、解析キャッシュ等を実施する。Phase 3はPhase 2の検証完了後にduplicate index、ad-filter index、storage batching、不要なBrowser API呼び出し削減等を実施する。各Phaseでは実装前確認、影響範囲確認、実装、静的検証、回帰確認、性能確認、未確認事項の記録を行い、検証完了前に次Phaseへ進まない。


### 245. 検出詳細設定は安全上限を持つ
Rule 239/240の解析予算をUIから調整可能にする場合、ユーザー設定値にも絶対上限を設ける。UI設定だけで検出性能保護を無効化したり、無制限解析へ変更したりできるようにしてはならない。

### 246. 解析設定の段階適用
検出詳細設定は軽量Precheck、重量解析、探索予算等の段階に対応させる。設定項目を追加しただけで未実装の解析処理を実装済みと扱ってはならない。各設定の実処理への接続状況を実装目録へ記録する。

### 247. Phase境界を越える実装を混在させない
Phase 1の検証完了前にPhase 2/3の重量解析、duplicate index、storage batching等を実装済みとして先行適用してはならない。ただし、後続Phaseで使用する設定UI・保存スキーマ等の準備だけを行う場合は、実処理への接続状態を明示する。

### 248. 解析予算超過時の安全終了
解析深度、ノード数、時間等の予算を超過した場合は、その解析単位を安全に打ち切り、検出系全体、既存内部リスト、他の検出経路を破壊してはならない。予算超過を動画不存在の証明として扱わない。


## 【UI起動ライフサイクル・rawCandidates】

### 249. UI表示前は受動ListenerとrawCandidates収集だけを行う
UIが完全表示される前は、DOM ListenerとNetwork Listenerによる受動検出を維持し、検出結果はrawCandidatesとして保持する。内部動画リストへの登録、内部リストの再構築、表リスト描画、履歴・ダウンロード状態の同期、CoApp連携、能動取得等を開始してはならない。

### 250. rawCandidatesと内部リストを完全分離する
rawCandidatesはUI/CoApp起動ゲート完了前の未確定候補であり、内部リストとは別の状態として管理する。rawCandidatesから内部リストへの移送はUI完全表示およびCoAppバージョン確認・必要な更新の完了後に一度の確定処理として行う。

### 251. UI完全表示後はCoApp確認を最優先する
UIが完全表示された後、動画リスト、ダウンロード状態、履歴、プレビュー等のリスト関連状態へ触れる前にCoAppバージョンを確認する。更新が必要な場合は更新ダイアログを表示し、ユーザー応答および更新完了確認が終わるまでリスト処理を開始してはならない。

### 252. 候補収集と重複判定を分離する
rawCandidatesは可能な限り全候補を収集した後、確定処理で1件ずつ既存候補と比較する。同一動画は既存項目へ統合し、別動画は内部リストへ追加する。表リストは確定した内部リストからのみ生成する。

### 253. UI起動ゲート前のBackground初期化を禁止する
UI起動ゲート完了前に、storage migration、広告フィルター初期化・更新、定期フィルター処理その他のリスト関連初期化を自動開始してはならない。UI起動ゲート完了後に必要な初期化を開始する。

### 254. UI表示後のCoApp更新中も動画候補収集を失わない
CoApp確認・更新中に到着する検出候補はrawCandidatesへ保持し、更新処理によって破棄しない。CoApp更新完了確認後に確定処理へ移送する。


### 255. 起動後処理は内部リスト表示後に開始する
CoAppバージョン確認・必要な更新が完了した後も、rawCandidatesの確定、内部リスト構築、表リスト表示を先に完了させる。storage migration、広告フィルター更新、履歴・ダウンロード同期等の非必須起動後処理は、表リスト表示後の非同期後処理として開始する。


### 256. 配布ZIPのルートはバージョン親フォルダーを作らない
配布ZIPを展開した直後に、さらに `Universal-Video-Detector-<version>/` 等の親フォルダーを作成してはならない。ZIPのルート直下に `manifest.json`、既存の `adapters/`、`background/`、`companion/`、`content/`、`ui/` および既存の必要ファイルを直接配置する。既存プロジェクト内の必要なサブフォルダーは維持するが、配布ZIP用の追加ラッパーフォルダーは作成しない。

### 257. Firefox負荷警告を優先して受動検出処理を軽量化する
Firefoxの拡張機能負荷警告につながる可能性がある受動検出処理について、検出経路を削除せず、本文読み取り、全DOM走査、不要なrequest body複製、広範囲なプレビュー先読み、画面外UI描画等のコストを優先して削減する。


### 258. コード構造を常に整理された状態に維持する
コード追加・修正時は既存処理との責務境界を明確にし、同一責務の処理を複数箇所へ分散させない。長大な一行処理、意味のない重複、不要な一時変数、到達不能コード、使用されない定数・関数を残さない。既存機能を維持したまま、誰が読んでも処理順序と責務を追える構造にする。

### 259. 変数・状態・設定値には目的を明示する
変数、定数、Map、Set、状態オブジェクト、設定項目等を追加・変更する場合、その用途がコードから明確に分かるコメントまたは自己説明的な命名を必ず付ける。特にモジュール共有状態、上限値、タイマー、キャッシュ、通信状態、解析予算等は「何を保持し、なぜ必要か」を直前または同一宣言内で説明する。単純なループ変数等まで冗長なコメントを増やしてコードを読みにくくしてはならないが、意味を推測しないと理解できない変数を無説明で追加してはならない。

### 260. スパゲティ化を禁止する
関数間の責務を明確に分離し、UI、検出、候補収集、重複判定、CoApp通信、永続化、診断を相互に直接依存させすぎない。新しい処理は既存の適切な責務へ接続し、同じ判定や保存処理を別名で複製しない。大きな関数へ条件分岐を継ぎ足し続けず、意味のある補助関数へ分割する。

### 261. 可読性を検証対象にする
Phaseごとの静的検証では構文・JSONだけでなく、未使用変数、未使用定数、重複した処理、過度な一行化、コメント不足、責務混在を確認する。可読性を損なう実装を「動くから」という理由だけで正式成果物へ含めない。

### 262. 今回の検出チューニング設定はPhase境界を守る
検出チューニング設定・プリセット・Save/LoadはPhase 1でUI/保存スキーマを完成させてよい。ただしPhase 2/3に属する重量解析予算、解析キャッシュ、duplicate index等の実処理はPhase 1検証完了前に有効化しない。UI上で保持する値と実処理への接続状態を実装目録へ明示する。

### 263. サイトアクセス制御を検出リスナー単位で適用する
ホワイトリスト/ブラックリストは検出対象サイトを制御する設定として扱う。ホワイトリスト選択時は指定サイト以外でDOM、Network、Mutation、Adapter等の動画検出リスナーを起動してはならない。ブラックリスト選択時は指定サイトでこれらの検出リスナーを起動してはならない。

### 264. サイトアクセス制御とCoApp通信を分離する
ホワイトリスト/ブラックリストによって動画検出を停止している場合でも、CoAppの接続確認、バージョン確認、必要な更新確認・更新処理は通常どおり実行できなければならない。サイトアクセス制御を理由にCoApp管理処理を停止してはならない。

### 265. サイト判定はホスト名・Originを安全に扱う
サイト指定値は正規化し、ホスト名、Origin、必要に応じてサブドメイン指定を安全に比較する。URL文字列の単純な部分一致だけで許可/拒否を判定してはならない。

### 266. 検出設定の初期値を仕様として固定する
待機時間の初期値は高速、試行回数は2回、最大検出時間は15秒とする。広告フィルターは初期状態OFF、外観プレビューは画像を初期値とし、動画プレビューは一時的に提供しない。

### 267. 設定保存はUI終了より先に永続化を開始する
設定変更時はFirefox storage.localへの保存を最優先し、Background通信や付随処理より前に永続化を開始する。Popupを閉じる操作が直後に行われても、変更値が旧設定へ戻らないようにする。

### 268. UIのクリック判定領域を必要最小限にする
詳細設定等のチェックボックスは、ラベル全体を不必要に大きなクリック領域として扱わない。誤操作防止のため、チェックボックスとその説明文を明確に分離し、横方向へ過度に広い判定領域を作らない。

### 269. バージョン形式は常に x.y.z の3要素で固定する
Universal Video Detector本体、CoApp、manifest、更新判定、設定スキーマ、成果物名等で使用する正式バージョンは必ず `x.y.z` の3要素で表記する。`x.y` の2要素表記へ変換・短縮・読み替えを行ってはならない。バージョン比較も3要素の正式値を基準に行い、旧形式を正式な現行バージョンとして扱う互換変換を新たに導入しない。既存の旧形式を検出した場合は、正式形式へ移行するための明示的な更新・再セットアップ経路を用意し、更新判定を曖昧にしない。
